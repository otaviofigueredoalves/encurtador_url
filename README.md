# Como rodar o projeto?
- Na pasta /config veja o .example e transforme em config.php;
- Coloque as credenciais do seu banco;
- Importe o dump do banco ou rode o migration (ainda não foi feito);
- Digite no console, na pasta raiz: php -S localhost:8000 -t public.
